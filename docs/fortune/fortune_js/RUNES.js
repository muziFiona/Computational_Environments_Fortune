//this will be the script for the Runes structure, drawing, meaning, the results

let runes = [];

function RUNE(){
  for (let i = 0; i < 78; i+=3){
    
    //stroke(runes[i]* 200);
    line(i, 0, i, 100);
  }
  
}
